automate new projects
    react ? with typescript?
    next    ? with typescript?  
    express 
    node-cli

add new custom header.

organize/style cli menu/flags to group for like use cases

remove unused packages(octokit?) is octokit or .env variables supplying token for commits? probably .env
figure out why git push action is returning stderror instead of success


server backend? express?
    gmail push notifications